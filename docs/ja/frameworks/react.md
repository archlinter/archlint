# Reactのサポート

Reactコンポーネントは、伝統的なクラスやモジュールとは異なるアーキテクチャ特性を持っています。

## 主な機能

- **コンポーネントの認識**: 命名パターンとJSXの使用によってReactコンポーネントを識別します。
- **LCOMの無効化**: コンポーネントは本来UIの状態とレンダリングに重点を置いているため、低凝集度（LCOM）検出器を自動的に無効にします。
- **フック分析**: カスタムフックが共有UIロジックのエントリポイントであることを理解します。

## 推奨設定

```yaml
extends:
  - react
```
