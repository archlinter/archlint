# Мертвый код

**ID:** `dead_code` | **Степень критичности:** Низкая (по умолчанию)

Мертвый код — это именно то, о чем вы подумали: функции, классы или переменные, которые «живут» в вашем проекте, но на самом деле ничего не делают, потому что их никто не использует.

## Почему это «запах»

- **Лишняя ментальная нагрузка**: Разработчики не должны тратить время на рефакторинг или попытки понять код, который даже не запускается.
- **Ложная сложность**: Мертвый код раздувает API ваших модулей, делая их визуально сложнее и страшнее, чем они есть на самом деле.
- **«Призраки» в коде**: Это приводит к ситуациям типа «Я же думал, мы это удалили полгода назад», которые всплывают в самый неподходящий момент при отладке.

## Примеры

### Плохо

```typescript
// utils.ts
export const usedHelper = () => { ... };
export const unusedHelper = () => { ... }; // Отмечается как мертвый код

// main.ts
import { usedHelper } from './utils';
```

## Как исправить

1. **Удалите его**: Если код действительно не используется, лучшее решение — удаление.
2. **Отметьте как точку входа**: Если это часть публичного API или динамический импорт, добавьте его в `entry_points` в вашей конфигурации.

## Конфигурация

```yaml
# Опции правила
rules:
  dead_code:
    exclude:
      - '**/tests/**'
      - '**/temp/**'

# Глобальные опции (корневой уровень)
entry_points:
  - 'src/index.ts'
  - 'src/api/**/*.ts'
```

### Опции

#### Опции правила (`rules.dead_code`)

- `exclude`: Список glob-паттернов для игнорирования при поиске мертвого кода. Файлы, соответствующие этим паттернам, будут рассматриваться так, как будто они не существуют для анализа входящих зависимостей.

#### Глобальные опции (корневой уровень)

- `entry_points`: Глобальные точки входа, которые никогда не должны помечаться как мертвый код.

## ESLint правило

Этот детектор доступен как правило ESLint для получения обратной связи в реальном времени.

```javascript
// eslint.config.js
export default [
  {
    rules: {
      '@archlinter/no-dead-code': 'warn',
    },
  },
];
```

Смотрите [Интеграция с ESLint](/ru/integrations/eslint) для инструкций по настройке.
