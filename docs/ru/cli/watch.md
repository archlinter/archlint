---
title: watch
description: "Запустите archlint в режиме наблюдения для автоматического повторного анализа вашего проекта при каждом изменении файлов, обеспечивая непрерывную обратную связь."
---

# archlint watch

Команда `watch` запускает archlint в фоновом режиме и повторно анализирует ваш проект при каждом изменении файла.

## Использование

```bash
archlint watch [options]
```

## Опции

| Опция             | По умолчанию | Описание                                      |
| ----------------- | ------------ | --------------------------------------------- |
| `--debounce <ms>` | `300`        | Ожидание новых изменений перед перезапуском   |
| `--clear`         | `false`      | Очищать экран терминала перед каждым запуском |
| `-c, --config`    | `none`       | Путь к конфигурационному файлу                |

::: tip
Команда `watch` также поддерживает все опции команды [`scan`](/ru/cli/scan).
:::

## Примеры

### Живая обратная связь во время разработки

```bash
archlint watch --clear --debounce 500
```
