# 中心模块

**ID:** `hub_module` | **Severity:** Medium (default)

“中心模块”（Hub Module）是指在依赖图中充当中心点的模块，同时具有高 Fan-in（扇入）和高 Fan-out（扇出）。

## 为什么这是一种坏味道

中心模块是架构中危险的“单点故障”。因为有如此多的东西依赖于它们，而它们又依赖于如此多的其他东西，所以它们极其脆弱，且难以在不引起整个代码库连锁反应的情况下进行更改。

## 如何修复

打破中心！识别通过该中心的不同数据或控制路径，并将它们提取到独立的、更集中的模块中。
