# 大文件

**ID:** `large_file` | **严重程度:** 中 (默认)

识别那些大到可能需要有自己邮政编码的源文件。

## 为什么这是一种坏味道

超大文件是导航的噩梦。你花在滚动和查找符号上的时间比写代码还多。通常，一个2000行的文件只是三四个更小的逻辑模块披着同一件外套而已。它违反了单一职责原则，并且几乎肯定会导致合并冲突。

## 如何修复

将文件拆分为更小、更集中的模块。

## 配置

```yaml
rules:
  large_file:
    max_lines: 1000
```
