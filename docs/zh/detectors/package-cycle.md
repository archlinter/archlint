# 包循环依赖

**ID:** `package_cycle` | **Severity:** High (default)

检测整个包（包含 `package.json` 的文件夹或逻辑模块边界）之间的循环依赖。

## 为什么这是一种坏味道

包级别的循环依赖比文件级别的循环依赖更为严重。它们阻碍了正确的版本控制，使得独立发布包变得不可能，并表明系统模块化存在严重缺陷。

## 如何修复

重新评估包之间的边界。通常，包循环依赖意味着两个包实际上应该合二为一，或者应该提取出第三个包来存放共享代码。
