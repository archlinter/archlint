# 稳定依赖原则 (SDP)

**ID:** `sdp_violation` | **Severity:** Medium (default)

稳定依赖原则指出：“包之间的依赖关系应该朝向稳定的方向。”换句话说，稳定的（难以改变的）模块不应该依赖于不稳定的（易于改变的）模块。

## 为什么这是一个代码异味

如果一个稳定模块（许多其他模块都依赖它）依赖于一个不稳定模块，那么稳定模块就会变得难以改变，因为不稳定模块中的任何更改都可能影响它，进而影响其所有的依赖者。

## 如何修复

确保您的核心、稳定模块不依赖于易变模块。使用接口或抽象类来解耦它们。
