# 霰弹式修改 (Shotgun Surgery)

**ID:** `shotgun_surgery` | **严重程度:** 中 (默认)

霰弹式修改是那种令人恼火的情况：一个"简单"的变化需要你接触15个不同的文件。这就像试图通过修补一百个小洞来修复泄漏，而不是更换管道。

## 为什么这是一种坏味道

- **高摩擦力**: 每个小需求变更都会变成一个大工程。
- **容易漏掉某个地方**: 当逻辑散落在各处时，你几乎肯定会忘记更新其中某个文件，导致"幽灵 bug"。
- **破坏的封装**: 这是一个信号：单一职责已经逃离了它的模块，现在藏在你代码库的每个角落。

## 如何修复

- **整合职责**：使用 **Move Method** 或 **Move Field** 将相关逻辑合并到一个模块中。
- **引入参数对象 (Introduce Parameter Object)**：如果多个模块需要相同的数据集，将其分组为单个对象。
- **用对象替换数据值 (Replace Data Value with Object)**：如果许多模块处理相同的原始数据，将该数据及其行为封装到一个新类中。

## 配置

```yaml
rules:
  shotgun_surgery:
    severity: medium
```
