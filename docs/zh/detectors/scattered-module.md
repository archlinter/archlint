# 分散模块

**ID:** `scattered_module` | **Severity:** Medium (default)

识别一个“模块”（通常是一个文件夹或一个逻辑分组），其中的内部文件之间没有很好的连接，表明该模块只是代码的随机集合。

## 为什么这是一种坏味道

一个模块应该是高内聚的。如果其内部部分互不影响，那么它可能不是一个真正的模块，应该被拆分或重构。

## 如何修复

重新评估模块的用途。将代码分组到更具内聚性的模块中，或者将不相关的部分移动到它们实际被使用的地方。
