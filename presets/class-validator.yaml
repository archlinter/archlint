name: class-validator
description: class-validator - Decorator-based property validation for classes
version: 1
detect:
  packages:
    any_of:
      - 'class-validator'

rules:
  dead_symbols:
    contract_methods:
      ValidatorConstraintInterface: ['validate']
